#include <stdio.h>
#include <stdio.h>
#include <conio.h>
#define e 5

int list[e];


int main()
{
	int i,numeral = 0, arrey, j = 0, num = 1;
	printf("n = ");
	scanf_s("%i", &i);
	list[numeral] = i;
	numeral++;
	for (arrey = 0; arrey < e-1; arrey++)
	{
		scanf_s("%i", &i);
		if (i > list[numeral])
		{
			list[numeral] = i;
			numeral++;
			num++;
		}
	}
	printf("n = ");
	while (j < num)
	{
		printf("%i ", list[j]);
		j++;
	}
	return 0;
}
